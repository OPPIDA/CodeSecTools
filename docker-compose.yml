services:
  test:
    image: cstools_test
    build:
      context: .
      dockerfile: tests/Dockerfile

    tty: true
    stdin_open: true

    volumes:      
      - pytest-cache:/app/.pytest_cache
    
    environment:
      _TYPER_STANDARD_TRACEBACK: 1

volumes:
  pytest-cache: